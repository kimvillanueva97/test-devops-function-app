{
  "name": "test-azure",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "mocha test/**/*.spec.ts --recursive"
  },
  "dependencies": {
    "mocha": "^9.0.3"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "@types/assert": "^1.5.5",
    "@types/expect": "^24.3.0",
    "@types/mocha": "^9.0.0",
    "@types/node": "^16.4.13",
    "ts-mocha": "^8.0.0",
    "typescript": "^4.0.0"
  }
}
